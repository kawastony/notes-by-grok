# Theory open items

Tony Kawas / 10 September 2026. Updated after cone replacement of micro-geometry.

---

## Closed (locked)

| Item | Statement |
|---|---|
| Disk floor | \(a_T=8.25\times10^{-11}\,\mathrm{m\,s^{-2}}\) is the **only** calibrated galactic constant |
| Interpolator at transition | \(\nu(1)=\varphi\) exact |
| \((11/72)_{\mathrm{cone}}\) | \(\Delta y/\varphi\) |
| \(R_{\mathrm{cone}}\) | \(\sqrt{6}/\varphi\) |
| 10D dictionary for those numbers | **retired** |
| Volume constraint | \(N(2LA_\phi p+1)=0\) |
| Seasonal \(a_T\propto H_0/H\) | **disfavored** (MUSE-DARK) |

---

## A. Volume constraint (algebra closed)

\[
N\bigl(2 L A_\phi\, p + 1\bigr)=0.
\]

| Branch | Meaning | Sky |
|---|---|---|
| Pause \(N=0\) | field frozen on slope with \(V=\rho_\Lambda\) | identical to \(\Lambda\)CDM distances |
| Track | forced \(p\) | not a pass on DESI+SN without hierarchy |

**Open phenomenology:** one observable where pause differs from bare \(\Lambda\).

---

## B. Charge unit \(q_0\) (blocked classically)

Topology gives \(N_{\mathrm{def}}\in\mathbb{Z}\). Energy matching + Maxwell identification gave

\[
q_0\sim \varepsilon_\ast a c\,\varepsilon_0\sqrt{4\pi C}.
\]

Joint \(S_{\mathrm{core}}=\hbar\) and \(q_0=e\) forces \(\alpha=1\), not \(1/137\).

**Open:** non-classical step that does not force \(\alpha=1\). Not fixed by tuning \(a\).

---

## C. Microscopic length from 5D

\[
\Delta y=0.2445\,f/\Lambda^2.
\]

Primary role: late-time / slab clock **and** factor in \((11/72)_{\mathrm{cone}}\).  
Not the EM lattice scale (current reading).

---

## D. High-z acceleration scale

| Rule | Status |
|---|---|
| \(a_T(z)\propto H_0/H(z)\) | disfavored |
| constant \(a_T\) | mild tension with MUSE-DARK rise |
| \(a\propto H\) | same direction as data; **not derived** from pause |

**Open:** forced seasonal law compatible with intermediate-\(z\) RAR.

---

## E. What counts as a closed next equation

Acceptable:
- pause-branch observable ≠ \(\Lambda\)CDM, derived not fitted;
- \(q_0\) from topology + one scale already in the 5D notes without \(\alpha=1\);
- disk-floor link from cone magnitude \(\varepsilon_\ast\) to \(a_T\) without galaxy-by-galaxy numbers;
- high-\(z\) \(a_T\) forced by cone geometry and matching MUSE-DARK direction.

Unacceptable as “closed”:
- free \(f\), free \(\xi\), free \(\delta\) per galaxy, restored \(Q\), restored 10D dictionary as source of 11/72 or R.

---

## F. Status line

Disk floor: **empirically live** (sole calibrated galactic constant).  
Cone 11/72 and R: **locked as accounting**.  
DE pause: **logically live, sky-degenerate**.  
EM quantisation: **structurally open**.  
High-z seasonal law: **open** (first prototype failed).
