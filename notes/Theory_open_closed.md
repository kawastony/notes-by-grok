# Theory open items (item 3)

Tony Kawas / 10 September 2026. Closed statements of what is open — not new free parameters.

---

## A. Volume constraint (already solved algebraically)

\[
N\bigl(2 L A_\phi\, p + 1\bigr)=0.
\]

| Branch | Meaning | Sky |
|---|---|---|
| Pause \(N=0\) | field frozen on slope with \(V=\rho_\Lambda\) | identical to \(\Lambda\)CDM distances |
| Track | forced \(p\) | \(\Delta\chi^2\approx +58\) vs DESI+SN in prior note — not a pass |

**Open phenomenology:** name one observable where **pause** differs from bare \(\Lambda\) (structure growth, lensing slip, void statistics). Until then, pause is philosophically distinct and observationally degenerate with a constant.

---

## B. Charge unit \(q_0\) (blocked classically)

Topology gives \(N_{\mathrm{def}}\in\mathbb{Z}\). Energy matching + Maxwell identification gave

\[
q_0\sim \varepsilon_\ast a c\,\varepsilon_0\sqrt{4\pi C}.
\]

Joint \(S_{\mathrm{core}}=\hbar\) and \(q_0=e\) forces \(\alpha=1\), not \(1/137\).

**Open:** non-classical step or different action identification that does not force \(\alpha=1\). Not to be “fixed” by tuning \(a\).

---

## C. Microscopic length from 5D

Stable interval:
\[
\Delta y=0.2445\,f/\Lambda^2.
\]

Used as lattice spacing \(a\) does **not** yield \(e\) for TOE-motivated \((f,\Lambda)\).

**Open:** whether \(\Delta y\) is only a late-time / slab clock, not the EM lattice scale (current reading: yes, slab clock).

---

## D. What counts as a closed next equation

Acceptable:
- pause-branch observable ≠ \(\Lambda\)CDM, derived not fitted;
- \(q_0\) from topology + one scale already in the 5D notes without \(\alpha=1\);
- disk-floor link from cone magnitude \(\varepsilon_\ast\) to \(a_T\) without galaxy-by-galaxy numbers.

Unacceptable as “closed”:
- free \(f\), free \(\xi\), free \(\delta\) per galaxy, restored \(Q\).

---

## E. Status line

Disk floor: **empirically live.**  
DE pause: **logically live, sky-degenerate.**  
EM quantisation: **structurally open.**
