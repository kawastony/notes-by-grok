# Q2 — Density / warp-pressure residual near z∼0.3

Tony Kawas / 12 September 2026. Honest status vs frozen DE floor.

---

## What is actually derived

| Object | Status |
|---|---|
| Activation interval \(\Delta y\to z\sim0.3\) | Derived (O(1) drivers) |
| Pause branch: freeze, \(w=-1\), distances ΛCDM-like | Preferred ontology; SN/BAO degenerate with B1 |
| Bulk \(T_{yy}\) residual as density/pressure correction | Exists in unit chart (\(\varepsilon\sim\mathcal{O}(1)\)); projects to isotropic effective density, not slip |
| Homogeneous sector \(\eta=0\) | Theorem within block-diagonal sector (Q1) |

---

## What is **not** derived (do not promote)

| Claim from external analysis | Why not filed |
|---|---|
| \(w_0\approx-0.77\), \(w_a\approx-0.15\) | Not an output of the locked pause / B1 floor; B2 hierarchy cases were refused or not separable from ΛCDM at current precision |
| \(R\approx1.5156\) normalization jump as derived DE scale | Retired: 10D dictionary / cone accounting, not a 5D ODE eigenvalue |
| Local inflection \(dw/dz=0\) at \(z\approx0.35\) as forced signature | Pause ontology gives \(w=-1\) constant; no derived turning point without reopening B2 hierarchy |
| Automatic match of \(\Omega_{\mathrm{DE}}\), \(\Omega_{\mathrm{DM}}\) from warp Jacobian | Not computed from the action under freeze |

`DE_discriminator.md`: Δμ at \(z\sim0.3\) is millimag for the activation tick; it does **not** pick B1 vs B2, and pause is sky-identical to a constant on distances.

---

## Honest answer to Q2

**Does the residual leave a measurable isotropic observable near \(z\sim0.3\)?**

- **In principle:** a bulk density residual *could* correct \(\rho_{\mathrm{eff}}(z)\) or the onset of acceleration.
- **As derived today:** under the preferred **pause** ontology, the expansion history is ΛCDM-degenerate. No forced, parameter-free \(w(z)\) turning point or \(H(z)\) inflection has been computed that differs from ΛCDM at a testable level without reintroducing refused hierarchy (B2, large \(f\)) or retired numbers (\(R=1.5156\)).

So the scalar residual is **alive as a channel** and **not yet a derived discriminator**. Promoting specific \(w_0,w_a\) or \(R\) values would reverse the freeze.

---

## What would make Q2 a real discriminator later

Only if **all** hold:

1. A reduction from the action produces a definite correction to \(\rho_{\mathrm{eff}}(z)\) or \(w(z)\) near the activation interval.
2. That correction is **not** absorbed into a constant \(\Lambda_0\) (otherwise distances stay degenerate).
3. No free scale is tuned to DESI/SN; failure is allowed.

Until then: clock yes, scalar residual yes as mechanism, **observable w(z) signature not claimed**.

---

## One-sentence close

The bulk residual is isotropic and real, but under the frozen pause floor it does not yet yield a derived, testable departure from ΛCDM distances or a forced \(w(z)\) turning point; specific numerical claims (\(w_0,w_a,R\approx1.5156\)) remain outside the constitution.
