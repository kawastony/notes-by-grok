# New forced-prediction search (post rate-gap acceptance)

Tony Kawas / 10 September 2026. Activation-rate gap accepted incomplete. New targets only.

---

## Tip vs waist (closed)

Not the cause of the instanton negative. See Tip_vs_waist_and_gap_accept.md.

---

## Candidates examined

### C7 — waist circumference ratio from volume constraint

**Fails as forced disk number.**  
\(N(2LA_\phi p+1)=0\) is cosmological (lapse/branch). It does not fix a local galactic circumference ratio.

### C8 — \(e^{A_{\mathrm{wall}}}=e^{-1/2}\) as observable factor

**Locked** as 5D wall value. Appears in \(M_{\mathrm{Pl}}^2=M_5^3\int e^{2A}\mathrm{d}y\) (wall suppressed by \(e^{-1}\)).  
**Not** a new disk RC number unless mapped into \(G\) or \(a_T\) — that map is Prediction-A-class coincidence risk.  
Numerically \(e^{-1/2}\approx 0.6065\) vs \(1/\varphi\approx 0.618\) (1.9%) — accounting-level only, same style as \((11/72)_{\mathrm{cone}}\).

### C3 — pause gravitational slip ≠ ΛCDM

**Shape of claim is right; coefficient not forced yet.**

From TAFA_stress_tensor_and_pressure.md:

- Homogeneous pause (\(\dot\phi=0\), \(V\to\mathrm{const}\)): \(T_{\mu\nu}=-V g_{\mu\nu}\) — **identical to Λ** for background and for anisotropic stress (**slip Φ−Ψ = 0**).
- Nonzero slip requires **inhomogeneous residual cone strain** (spatial gradients or defects).
- Absolute amplitude of that residual is not fixed by the locked floor (same class as cluster \(n_2\)).

**Verdict:** homogeneous pause is sky-degenerate with ΛCDM including slip. A pause-only discriminator needs a derived residual strain field — not available without reopening free density parameters.

### High-z \(a_T\) / clusters

Unchanged: seasonal disfavored; dynamic closure ~3%; Σ_res needs \(n_2\).

---

## What *is* still forced and usable

| Claim | Status |
|---|---|
| \(a_T\) calibrated; simple μ motivated (linear two-channel) | Locked / conditional |
| φ at transition under that package | Algebra |
| \(R_{\mathrm{cone}}\), \((11/72)_{\mathrm{cone}}\) accounting | Locked accounting |
| \(A\to-1/2\), \(e^{A}=e^{-1/2}\) | Locked 5D |
| Homogeneous pause ≡ Λ for background + slip | Forced — **limits** discriminators |
| Cone → Maxwell map | Mapping, given A≡ε |

---

## Recommended next work (different thread)

1. **External disk sample with full \(V_{\mathrm{bar}}\)** — pure empirical stress test of frozen \(a_T\) + simple μ; no new theory risk.
2. **If theory:** derive residual strain amplitude after freeze from the discrete cone + volume constraint **without** a free \(n_2\) — only path to slip ≠ 0 or Σ_res.
3. **Do not:** reopen rate linearity, promote A, dent δ, 10D dictionary.

---

## One-sentence program state

The activation-rate derivation is closed incomplete; homogeneous pause is Λ-like including slip; the next low-risk advance is an external rotation-curve sample, and the next high-risk theory advance is a parameter-free residual strain after freeze.
