# Pause-only hardened — residual from locked cone accounting

Tony Kawas / 11 September 2026. Layer C.

Uses **already locked** numbers only: \((11/72)_{\mathrm{cone}}\), \(R_{\mathrm{cone}}\), activation interval \(z_{\mathrm{tr}}\sim 0.3\). No new free parameters. No SPARC form reopening.

---

## Locked inputs (scorecard)

\[
\varphi = \frac{1+\sqrt{5}}{2},\qquad
\Bigl(\frac{11}{72}\Bigr)_{\mathrm{cone}} := \frac{\Delta y}{\varphi}\approx 0.1528,\qquad
R_{\mathrm{cone}}:=\frac{\sqrt{6}}{\varphi}\approx 1.51.
\]

5D tick: \(\Delta y = 0.2445\,f/\Lambda^2\) (unit normalizations) maps to late activation window \(z_{\mathrm{tr}}\sim 0.3\).

Pause: field frozen on slope, \(N=0\), \(V_A(\phi_{\mathrm{freeze}})=\rho_\Lambda\). Distances = flat ΛCDM (borrowed history — named limitation).

---

## Residual after freeze

Full freeze would erase wall stress. Cone accounting retains a **geometric residual** of relative size
\[
\varepsilon_{\mathrm{res}}\;\sim\;\Bigl(\frac{11}{72}\Bigr)_{\mathrm{cone}}\approx 0.15
\]
as the only locked dimensionless mismatch between the 5D tick and pure 4D freeze (\(\Delta y/\varphi\) is exactly that bookkeeping).

\(R_{\mathrm{cone}}\) is amplitude accounting on the same cone; it does not introduce a second free residual scale. Use one residual only: **11/72**.

---

## Map to slip (one identification)

Anisotropic stress from residual wall strain sources gravitational slip. In the quasi-static, late-time, scale-independent ansatz used by surveys:
\[
\eta\;:=\;\frac{\Phi}{\Psi}-1
\qquad\text{or conventions with}\quad
\gamma_{\mathrm{slip}}=\Phi/\Psi.
\]

**Provisional forced-order identification** (the step that resolves pure directionality):
\[
|\eta|_{z\,\lesssim\,z_{\mathrm{tr}}}\;\sim\;\Bigl(\frac{11}{72}\Bigr)_{\mathrm{cone}}\;\approx\;0.15
\]
with support mainly for \(z\lesssim 0.3\) (pause active). For \(z\gg z_{\mathrm{tr}}\) the field is not yet frozen; residual stress is not the pause residual — no claim there without a roll→disk map.

Sign: not fixed by 11/72 alone (polarity of wall stress). Report **amplitude class** \(|\eta|\sim 0.15\) at low \(z\).

This is still one identification (residual bookkeeping → slip amplitude). It is **not** a free η knob: the number is locked cone accounting. It **is** stronger than “η≠0 if strain exists.”

---

## Published bounds — is there room?

Recent combined-probe results (DESI DR1 + KiDS/DES/HSC; MG parameterizations with late-time Σ, μ, η):

- \(E_G\) measurements consistent with GR+Planck ΛCDM across \(0.15\lesssim z\lesssim 1\) (no strong detection of slip).
- Parameterized deviations: Σ₀, μ₀ consistent with GR at the ~0.05–0.2 level depending on dataset and parameterization; some analyses show mild Σ tension that is sensitive to CMB-lensing likelihood choices.
- Typical present uncertainty on late-time slip-like parameters is **order 0.05–0.2** (1σ-class), not 0.01.

**Room assessment:**

| Predicted \(|\eta|\sim 0.15\) at \(z\lesssim 0.3\) | Status |
|---|---|
| Already killed by published bounds? | **Not clearly** — sits near the edge of current sensitivity |
| Invisible forever? | **No** — Stage-IV (Euclid, LSST) targets much tighter slip |
| Directional-only forever? | **No** — amplitude is now specified from 11/72 |

So TAFA is **not cornered to death** by current data, and **not free to hide** at arbitrarily small η. The prediction is in the **testable band**.

---

## Competing table (updated)

| Model | \(H(z)\) | Late \(|\eta\|\ (\(z\lesssim 0.3\)) |
|---|---|---|
| Flat ΛCDM (GR) | ΛCDM | **0** |
| Simple-μ MOND + Λ | ΛCDM-like | **0** |
| **TAFA pause** | ΛCDM-like (borrowed) | **\(\sim 11/72 \approx 0.15\)** |

---

## What this resolves from the critique

| Critique point | Response |
|---|---|
| Directional only / “if residual exists” | Residual size locked to 11/72; amplitude class fixed |
| Null bound kills only with amplitude | Now a null at \(|\eta|\ll 0.05\) at low z would pressure the identification |
| H(z) borrowed | Named: still true; slip is the channel distances do not fix |
| Bounds before map | Bounds surveyed; room exists at ~0.15; map order-of-magnitude from accounting |

---

## Remaining soft spot (named)

The step
\[
\varepsilon_{\mathrm{res}}\sim\frac{11}{72}\quad\longrightarrow\quad|\eta|\sim\frac{11}{72}
\]
is an **order-unity identification**, not a derived transport coefficient from the cone stress tensor. A future derivation of Σ[ε_res] from the volume-constraint residual would replace “~” with a calculable prefactor. Until then, quote **\(|\eta|\sim 0.15\)** as accounting-level, not action-level.

---

## Do not

- Reopen SPARC form derivation.
- Treat z=12 as derived cosmology.
- Promote Prediction A.
- Claim H(z) is predicted by pause.

---

## One-sentence close

Using locked cone accounting, the pause residual is set to \((11/72)_{\mathrm{cone}}\approx 0.15\); identifying that residual with late-time gravitational slip gives **\(|\eta|\sim 0.15\) for \(z\lesssim 0.3\)** at fixed ΛCDM distances — a quantitative, MOND-distinct, currently testable-band prediction whose main remaining soft spot is the order-unity residual→slip prefactor, not the absence of an amplitude.
